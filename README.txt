1. Run `vercel dev` to launch this website locally.
2. The whole website relies on a single style.css CSS file. Make changes only in that file and do not create more CSS files.
3. Refer to the template folder and commented sections on homepage for original template that was used.